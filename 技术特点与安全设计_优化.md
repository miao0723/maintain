# 第九章 技术特点与创新点

## 9.1 接口与架构设计
- **RESTful 资源化设计**：以资源路径组织接口，使用 HTTP 动词（GET/POST/PUT/DELETE）表达语义，可读性强、易于扩展，便于第三方系统集成。
- **统一响应约定**：后端接口统一以 `{ success, data, message }` 结构返回（前端解析层同时兼容 `{ code, message, data }` 风格），配合集中式错误处理中间件，前端统一拦截与提示，解析逻辑高度复用。
- **中间件化横切关注点**：鉴权（`middleware/auth`）、图片压缩（`sharp`）、异常处理（`express-async-errors` + 统一错误中间件）均以中间件实现，业务代码保持简洁内聚。
- **多端部署适配**：`/api` 与 `/mp-api` 双前缀兼容，线上由 Nginx 将 `/mp-api` 反代至后端 `/api`，本地调试与线上网关无缝切换。

## 9.2 数据安全机制
- **身份认证**：JWT 无状态令牌鉴权，统一由 `middleware/auth.js` 校验令牌有效性；用户密码经 bcrypt 加盐哈希存储，杜绝明文落库。
- **权限隔离**：基于角色的访问控制（RBAC），管理端接口按 `req.user.role` 进行角色权限校验，客户端与管理端逻辑相互隔离。
- **密钥管理**：大模型 API Key、数据库密码、微信 AppSecret、支付密钥等统一存放于 `.env`，经 `dotenv` 注入环境变量，源码零硬编码。
- **传输安全**：客户端全程 HTTPS，生产环境由 Nginx 终止 TLS；支付回调报文经微信支付证书验签，防止伪造。
- **文件安全**：`multer` 限定上传文件类型与大小，`sharp` 压缩图片，防范恶意文件与超大资源占用。

## 9.3 性能优化策略
- **连接池复用**：`mysql2` 连接池复用数据库连接，减少频繁建连开销，支撑高并发访问。
- **图片压缩**：`sharp` 对上传图片进行有损/无损压缩，降低存储成本与传输带宽。
- **静态资源分离**：上传文件存储于独立目录，经 Nginx 直接静态服务，减轻应用服务器压力。
- **实时推送替代轮询**：基于 WebSocket（`socketHub`）按需推送消息，避免高频轮询带来的无效请求。
- **索引与聚合优化**：对高频查询字段建立索引，统计类接口采用聚合查询，保证管理看板响应速度。

## 9.4 多智能体 AI 架构（创新点）
系统创新性地采用"路由智能体 + 领域智能体"的分层 AI 架构：`routerAgent` 负责意图识别与任务分发，将用户请求路由至 `faqAgent`（知识库问答）、`queryAgent`（订单/报价查询）、`repairRecycleAgent`（维修/回收咨询）等专业化智能体；各智能体聚焦单一领域，提示词可控、输出稳定。同时引入 `enhancedChatAgent` 作为增强兜底，结合知识库与业务数据的检索增强（RAG）输入，有效抑制大模型幻觉，显著提升客服准确率与响应效率。

## 9.5 高可用与容错
- **统一异常处理**：`express-async-errors` 捕获异步异常，避免进程崩溃；错误信息经统一中间件收敛后返回，不泄露内部细节。
- **健康检查**：提供 `/health` 与 `/db-health` 接口，实时检测服务与数据库连通性，便于监控告警与探活。
- **容器化与守护**：基于 Docker Compose 编排部署，服务异常自动重启（restart 策略），保证可用性。
- **环境隔离与降级**：关键配置经 `.env` 注入，可快速切换环境（本地/线上）与第三方服务端点，无需改动代码。

---

# 第十章 安全设计
- **身份认证**：JWT 令牌鉴权，统一由 `middleware/auth.js` 校验令牌有效性；管理端接口按角色（`role`）二次校验权限。
- **密码与凭证**：用户密码经 bcrypt 加盐哈希存储，不保存任何明文。
- **密钥管理**：大模型 API Key、数据库密码、微信 AppSecret、支付密钥等统一存放于 `.env`，经环境变量注入，源码中不硬编码任何密钥。
- **文件上传**：`multer` 限定文件类型与大小，`sharp` 压缩图片，防范恶意文件与超大资源。
- **传输安全**：客户端经 HTTPS 访问，生产环境由 Nginx 终止 TLS；支付回调报文经微信支付证书验签。
- **输入与错误处理**：集中式错误处理中间件统一收敛异常，避免将内部堆栈、SQL 等细节直接暴露给客户端。
- **权限控制**：接口级 RBAC 校验 + 前端按角色控制入口，双层防线防止越权访问。
- **审计留痕**：订单状态流转、报价、审批等关键操作全程留痕，可追溯、可审计。
