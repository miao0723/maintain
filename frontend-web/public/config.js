// ============================================================
// 运行时配置：上传到服务器后可直接修改本文件，无需重新构建前端
//
// apiBase 说明：
//   1. 同源部署（推荐）：前端和 /api 网关在同一个域名下，保持 '/api'
//   2. 跨域部署：填完整地址，如 'https://api.example.com/api'
//      （注意结尾不要带斜杠；跨域时后端需允许 CORS）
// ============================================================
window.__APP_CONFIG__ = {
  apiBase: '/api'
}
